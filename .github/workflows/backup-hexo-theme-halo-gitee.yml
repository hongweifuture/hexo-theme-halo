# workflow name
name: Backup hexo-theme-halo to Gitee

# master branch on push, auto run
on: [push]

# Private_Keys      
jobs:
  backup:
    name: Backup hexo-theme-halo To Public Project 
    runs-on: ubuntu-latest 
    env:
      TZ: Asia/Shanghai  
          
    steps:
    # check it to your workflow can access it
    # from: https://github.com/actions/checkout
    - name: Checkout Repository master branch
      uses: actions/checkout@master 
      
    - name: Setup Git Infomation
      run: | 
        git config --global user.name 'hongweifuture' 
        git config --global user.email 'hongweifuture@163.com'  
 
    - name: Get Latest Commit Message 
      run: |
        git log --pretty=format:"%s from Github Actions at `date +"%Y-%m-%d %H:%M:%S"`" --date=short -n 1  > commit-message.log
        
    - name: Setup SSH Private Key
      env:
        token_Private_Keys: ${{ secrets.Private_Keys }}
      run: |
        mkdir -p ~/.ssh/
        echo "$token_Private_Keys" > ~/.ssh/id_rsa 
        chmod 600 ~/.ssh/id_rsa
        ssh-keyscan github.com >> ~/.ssh/known_hosts
        ssh-keyscan gitee.com >> ~/.ssh/known_hosts
        
    - name: Backup Theme To Gitee Public Project hexo-theme-halo
      env:
        Gitee_Hexo_halo: gitee.com:hongweifuture/hexo-theme-halo.git
      run: |
        git clone git@${Gitee_Hexo_halo} .gitee_hexo_halo
        cd .gitee_hexo_halo
        git checkout master
        cd ../
        \cp -rf ./themes/halo/* ./.gitee_hexo_halo/
        \cp -rf ./themes/halo_extend/* ./.gitee_hexo_halo/
        \cp -rf ./.git/ ./.gitee_hexo_halo/
        cd .gitee_hexo_halo
        git add .
        git commit -F ../commit-message.log
        git fetch --unshallow origin
        git push --force --quiet "git@${Gitee_Hexo_halo}" master:master
        
 